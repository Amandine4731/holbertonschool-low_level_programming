#include "main.h"

/**
 * _abs - print the absolute value of the number
 * @n: the value to check
 * Return: n >= 0 = n, n < 0 = -n
 */
int _abs(int n)
/* prototype for the function */
{
	if (n >= 0)
	{
	return (n);
	}
	else
	{
	return (-n);
	}
}
