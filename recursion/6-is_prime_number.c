#include "main.h"
/**
 * is_prime_number - prime numbers
 * @n: a variable
 * Return: 0 if is not a prime number, 1 if is
 */
int is_prime_number(int n)
{
	if (n / 2)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
