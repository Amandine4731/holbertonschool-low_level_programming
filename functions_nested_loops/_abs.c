#include <stdlib.h>
/**
 * _abs - compute an absolute value
 * @n: The value to check
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _abs(int n);
{
	                return (write(1, &c, 1));
}
