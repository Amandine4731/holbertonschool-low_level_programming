#include "main.h"
/**
  * _islower - Entry point
  * Return: Always 0.
  */
int _islower(int c)
/* prototype for the function */
{	
	if(ch >= 'a' && ch <= 'z')
	{
		return (1)
	}
	else
	{
		return (0);
	
	}
}
