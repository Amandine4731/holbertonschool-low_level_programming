#include <ctype.h>
#include <locale.h>
/**
* _ialpha - checks for an alphabetic character
* @c: The character to check
*
* Return: On success 1.
* On error, -1 is returned, and errno is set appropriately.
*/
int _isalpha(int c);
{
		return (write(1, &c, 1));
}
