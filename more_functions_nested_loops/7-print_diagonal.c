#include "main.h"
/**
  * prin_diagonal - draws a diagonal line on the terminal
  * @n: to check
  */
void print_diagonal(int n)
{
	int r;
	char s = '\\';

	for (r = 0; r < n; r++)
	{
	if (n <= 0)
	{
	}
	else
	{
	_putchar(s);
	}
	_putchar('\n');
}
}
