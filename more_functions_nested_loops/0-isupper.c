#include "main.h"
/**
  * _isupper - check for uppercase letter
  * @c: the character to check
  * Return: 1 if c uppercase, 0 otherwize
  */
int _isupper(int c)
{
	if (c >= 'A'; c <= 'Z'; c++)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
