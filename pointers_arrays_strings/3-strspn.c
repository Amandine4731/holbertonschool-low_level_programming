#include "main.h"
/**
  * _strspn - get the length of a prefix substring
  * @s: a pointer
  * @accept: a pointer
  */
unsigned int _strspn(char *s, char *accept)
{
unsigned int i = 0;

if 
s[i] = s[0];

return
}
